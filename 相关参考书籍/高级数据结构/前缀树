前缀树: 字典树
字典查找:给定一系列字符串构成的字典,找出以""开头的字符串
暴力搜索法:O(M*N)
前缀树:O(M)

经典应用:
  搜索框补全,输入联想

性质:
  每个节点至少包含两个基本属性
  1.children: 数组或集合,罗列出每个分支包含的所有字符
  2.isEND: 布尔值,表示该节点是否为某字符串的结尾

  根节点是空的:
  除了根节点,其他所有节点都可能是单词的结尾,叶子节点一定都是单词的结尾

基本操作:
  创建:
    遍历一遍输入的字符串,对每个字符串的字符进行遍历
    从前缀树的根节点开始,将每个字符加入到节点的children字符集当中
    如果字符集已经包含了字符,跳过
    如果当前是字符串的最后一个,把当前节点的isEND标记为真

  搜索:
    从前缀树的根节点出发,逐个匹配输入的前缀字符
    如果遇到了,继续往下一层搜索
    没遇到,立即返回
